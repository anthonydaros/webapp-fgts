export enum DocumentType {
  RG = 'RG',
  CNH = 'CNH'
}

export enum BankAccountType {
  CHECKING = 'CHECKING',
  SAVINGS = 'SAVINGS',
  PIX = 'PIX'
}

export enum PixKeyType {
  CPF = 'CPF',
  EMAIL = 'EMAIL',
  PHONE = 'PHONE',
  RANDOM = 'RANDOM'
}

export enum Role {
  ADMIN = 'ADMIN',
  BROKER = 'BROKER',
  SUPPORT = 'SUPPORT',
  USER = 'USER'
} 